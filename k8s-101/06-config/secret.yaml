# Using `stringData` lets us write plaintext; K8s base64-encodes for us.
# Don't check real secrets into git — this is for local learning only.
apiVersion: v1
kind: Secret
metadata:
  name: db-creds
type: Opaque
stringData:
  DB_USER: admin
  DB_PASS: s3cret
